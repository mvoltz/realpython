base = input('Enter a base number: ')
exponent = input('To the power of: ')

print('{} to the power of {} = {}'.format(base, exponent, int(base) ** int(exponent)))

'''
Now we're exercising inputs, substitution, and converting strings to ints
'''

